[
  {
    "name": "a7_trapv_notrap",
    "hex": "tests/timing/a7_trapv_notrap.hex",
    "target_pc": "0x200",
    "expect_r": 0,
    "expect_p": 1,
    "expect_w": 0,
    "desc": "EXCEPTION_RELATED 'TRAPV (No Trap)': TRAPV (No Trap) NCC=4(0/1/0) -- V=0 (ccr cleared) -> TRAPV does not trap, falls through normally; watch_kind=3 (retirement pulse, no marker needed)",
    "instr_len": 2,
    "watch_kind": 3
  },
  {
    "name": "a7_bkpt",
    "hex": "tests/timing/a7_bkpt.hex",
    "target_pc": "0x200",
    "watch_reg": 2,
    "watch_val": "0xcafebabe",
    "expect_r": 1,
    "expect_p": 1,
    "expect_w": 0,
    "desc": "EXCEPTION_RELATED 'BKPT': BKPT NCC=9(1/0/0) -- Phase 157's own BKPT implementation issues a real CPU-space (FC=111) read for the DSACK'd bus-protocol outcome, confirmed via direct AS-fall trace. reliable-baseline plan: tb/timing_tb.sv's own is_data_fc classification now recognizes FC=111 as a countable read (it previously didn't, a genuine harness completeness gap -- expect_r used to read 0, not the true r=1), so this is now a real, complete r/p/w check. Phase 157's own known no-live-substitution scope boundary is a separate, already-documented limitation, unrelated to this fix.",
    "instr_len": 2
  },
  {
    "name": "a7_trap_n",
    "hex": "tests/timing/a7_trap_n.hex",
    "target_pc": "0x200",
    "watch_reg": 2,
    "watch_val": "0xcafebabe",
    "expect_r": 1,
    "expect_p": 2,
    "expect_w": 2,
    "desc": "EXCEPTION_RELATED 'TRAP #n': TRAP #n NCC=20(1/2/4) -- vector 32 (addr $80) points at trap_handler; TRAP #0 always dispatches there via a real exception-frame push. Measured w=2, not the manual's own row value of 4 -- confirmed via direct trace that this RTL pushes the 4-word (8-byte) frame as TWO longword (siz=00) writes at $FFFC/$FFF8, not four word writes; Harte's own exception-frame tests are 100% passing (final memory content verified correct), so this is a genuine write-GRANULARITY divergence from the manual's own assumed bus-cycle shape, not a correctness issue",
    "instr_len": 2
  },
  {
    "name": "a7_illegal",
    "hex": "tests/timing/a7_illegal.hex",
    "target_pc": "0x200",
    "watch_reg": 2,
    "watch_val": "0xcafebabe",
    "expect_r": 1,
    "expect_p": 2,
    "expect_w": 2,
    "desc": "EXCEPTION_RELATED 'Illegal Instruction': Illegal Instruction NCC=20(1/2/4) -- vector 4 (addr $10) points at illegal_handler; the real 68k ILLEGAL opcode ($4AFC) always dispatches there via a real exception-frame push. Measured w=2, not the manual's own row value of 4 -- same longword-vs-word write-granularity divergence as a7_trap_n, confirmed the identical way",
    "instr_len": 2
  }
]
